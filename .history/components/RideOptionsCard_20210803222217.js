import React from "react";
import { SafeAreaView, Text, View } from "react-native";

const RideOptionsCard = () => {
  return (
    <SafeAreaView>
      <Text style={tw` text-center py`}>Select a Ride</Text>
    </SafeAreaView>
  );
};

export default RideOptionsCard;

const styles = StyleSheet.create({});
